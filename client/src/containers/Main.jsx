import React from "react";
import { Header } from "../components";

const Main = () => {
  return (
    <main className="w-screen min-h-screen flex justify-center items-center flex-col bg-primary">
      <Header />
    </main>
  );
};

export default Main;
